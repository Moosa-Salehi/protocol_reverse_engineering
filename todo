- Use the extracted features (lengths, entropy vectors, byte histograms, uniqueness ratios, and motif/repetition features) in the project.
- Add repetition-pattern detection: repeated subsequences, trailing data blocks, fixed-vs-variable length profiles, and frequent byte n-grams.
- Improve message reconstruction so the PCAP path can optionally use true TCP stream reconstruction rather than packet-payload extraction only.
- Add an LLM-prep exporter that writes one compact evidence bundle per family: template, segment stats, top examples, request/response links, echoed fields, length relations, and semantic candidates.
- Add evaluation/reporting for the implemented stages: family coverage, cluster size distribution, boundary confidence summaries, and parseability of discovered families.
- beautifull html (dark mode, using css, perfect ui/ux) report (protocol spec) generation.
- merge stages 1 to 3, 1 script must create 01_messages.jsonl from the folder containing pcaps.